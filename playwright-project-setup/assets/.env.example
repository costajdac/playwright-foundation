# Base URL used by UI tests
BASE_URL=http://localhost:3000

# Base URL used by API tests (falls back to BASE_URL if unset)
API_BASE_URL=http://localhost:3000/api

# Add any auth tokens / secrets needed for API tests below.
# Never commit the real .env file — only .env.example.
# API_TOKEN=
